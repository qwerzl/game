export const useEegConfigStore = defineStore('eegConfigStore', {
  state: () => ({
    enabled: false,
    // TODO: Add more EEG options (Bluetooth mac address, etc)
  }),
  actions: {
    // TODO: Actions
  }
})
